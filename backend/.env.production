# Production Environment Variables
# IMPORTANT: Replace all placeholder values with actual production values
# NEVER commit this file with real credentials to version control

# Server Configuration
PORT=3001
NODE_ENV=production

# Supabase Configuration
# SECURITY: Get these values from your Supabase project settings
# Project URL: https://app.supabase.com/project/_/settings/api
# Format: https://xxxxx.supabase.co
SUPABASE_URL=https://your-production-project-id.supabase.co

# SECURITY: Anon key is safe for client-side use (public key with RLS policies)
# This key respects Row Level Security policies
SUPABASE_ANON_KEY=REPLACE_WITH_PRODUCTION_ANON_KEY

# SECURITY: Service role key has elevated privileges - NEVER expose to clients
# This key bypasses Row Level Security - use only in backend/server code
# Treat this like a database password - keep it secure and rotate regularly
# NEVER log this key or include it in error messages
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_PRODUCTION_SERVICE_ROLE_KEY

# Supabase Database Connection (for Prisma)
# SECURITY: Use connection pooling for production (add ?pgbouncer=true for better performance)
# Format: postgresql://postgres:[password]@db.[project-id].supabase.co:5432/postgres?pgbouncer=true
# Get from: https://app.supabase.com/project/_/settings/database
# Use "Transaction" mode connection string for Prisma
DATABASE_URL=postgresql://postgres:REPLACE_WITH_DB_PASSWORD@db.your-production-project-id.supabase.co:5432/postgres?pgbouncer=true

# Supabase Storage Configuration
# The name of your storage bucket for drawings
# Create this bucket in Supabase Dashboard: https://app.supabase.com/project/_/storage/buckets
# Configure bucket policies for appropriate access control
SUPABASE_STORAGE_BUCKET=excalidraw-drawings-prod

# JWT Configuration
# SECURITY: Generate with: openssl rand -base64 64
# Use a different secret than development
JWT_SECRET=REPLACE_WITH_STRONG_JWT_SECRET_MIN_64_CHARS
JWT_EXPIRES_IN=7d

# CORS Configuration
# Set to your actual production frontend domain
# Use HTTPS in production
FRONTEND_URL=https://app.yourdomain.com

# Optional: Email service for password reset (if implemented)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=REPLACE_WITH_SENDGRID_API_KEY
# SMTP_FROM=noreply@yourdomain.com
